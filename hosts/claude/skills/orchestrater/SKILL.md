---
name: orchestrater
description: Use Orca-backed orchestrater coordination after the local runtime CLI has been installed. Use for supervised multi-agent work in the current worktree.
disable-model-invocation: true
---

# Orchestrater

Read the installed package's `SKILL.md` and use its RuntimeAdapter workflow. Do not dispatch a write task without a successful CLI preflight and evidence capture.
